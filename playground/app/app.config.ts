export default defineAppConfig({
  ui: {
    primary: 'emerald',
    container: {
      constrained: 'max-w-2xl'
    },
    card: {
      defaultVariants: {
        variant: 'subtle'
      }
    }
  }
})
